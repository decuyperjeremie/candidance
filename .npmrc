# Pin installs to the public npm registry so `npm ci` works on any machine.
# Without this, a machine configured for an internal registry (e.g. a corporate
# Artifactory) would resolve and lock packages against that private mirror,
# breaking installs anywhere else.
registry=https://registry.npmjs.org/
